import React from 'react';

import { useNavigate } from 'react-router-dom';

export const About = () => {
    const goTo = useNavigate();
    return (
        <div>
            
        </div>
    );
};

